{
  "name": "art-party-deploy-trigger",
  "version": "1.0.0",
  "description": "Example of creating a function that runs as a cron job using the serverless `schedule` event",
  "author": "",
  "scripts": {
    "local": "serverless invoke local --function checkAndTriggerDeploy"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.8"
  }
}
